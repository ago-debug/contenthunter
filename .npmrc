# Tailwind v4 (@tailwindcss/oxide): i binari per OS/arch sono optional deps.
# Non usare `npm install --omit=optional` in deploy. Installare sempre sul server Linux (mai copiare node_modules da altri OS).
